/** Minimum unlock price creators may set (USDC). */
export const MIN_POST_PRICE_USDC = 0.001;

/** Soft cap on paywalled body length (characters). Large reports with SVG stay well under this. */
export const MAX_POST_BODY_CHARS = 500_000;