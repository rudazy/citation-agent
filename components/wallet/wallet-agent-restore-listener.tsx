"use client";

/**
 * Reserved for future wallet event hooks. Restore runs only after explicit
 * connect actions (Connect wallet buttons), not on page load.
 */
export function WalletAgentRestoreListener() {
  return null;
}